Steps To Install Project Files

1)Install Python
)Open command line and do the following with internet connection:
pip install flask
pip install kenobi
pip install flask-bcrypt

After installation run project by opening command line in project folder and type
python main.py

open browser and go to http://localhost:5000